# Crash dumps (local only)

Run **`collect-crash.bat`** after a Palworld crash. It copies dumps + UE4SS logs into a timestamped folder here and makes a `.zip` you can send.

These files are **not** committed to git (too large). Share the zip with whoever is debugging.
